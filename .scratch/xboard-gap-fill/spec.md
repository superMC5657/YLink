# YLink 功能补齐 · 对齐 Xboard 缺口（Spec）

> Status: needs-triage
> 日期: 2026-08-27
> 来源: YLink vs [cedar2025/Xboard](https://github.com/cedar2025/Xboard)（V2Board 系）功能对比盘点
> 范围: Xboard 有而 YLink 缺失或深度不足的功能，按优先级立项；YLink 独有能力（Tauri 桌面端/Go 栈/可观测性/代理审批）不在本文档范围

---

## 1. 背景与目标

YLink 已完成核心交易闭环（账户/套餐/订单/支付/订阅/邀请/工单/管理端 13 模块）并投入联调，但与 Xboard 相比缺少一批**商业化外围能力**与**管理端深度操作**：

- 缺失独立模块：礼品卡、佣金提现、多支付渠道、插件/主题/在线更新、Telegram 机器人、运维监控界面等；
- 已有但深度不足：统计报表、批量操作、CSV 导出、手动标记支付等。

本 spec 的目标是逐项补齐这些缺口，形成与 Xboard 对等（或按商业场景取舍）的能力矩阵。

## 2. 对比口径

- 功能清单来源：Xboard `app/Http/Controllers/V1|V2` 全部控制器方法、`v2_*` 数据表、`plugins-core/` 支付插件目录、`theme/` 主题机制，对照 YLink `server/internal/router/router.go` 全量端点、`src/views` 前端页面、`server/migrations` 表结构。
- 优先级口径：P0 = 直接影响商业变现与营收闭环；P1 = 明显提升运营效率与数据决策；P2 = 生态/体验类，成本高、可缓排。

## 3. 需求清单（总览）

| 编号 | 需求 | 优先级 | 模块 | 状态 |
|---|---|---|---|---|
| F01 | 礼品卡/兑换码系统 | P0 | 交易 | 未开始 |
| F02 | 佣金提现（工单提现） | P0 | 营销/工单 | 未开始 |
| F03 | 多支付渠道接入 | P0 | 支付 | 未开始 |
| F04 | 管理端统计报表增强 | P1 | 管理端 | 未开始 |
| F05 | 用户管理增强（CSV 导出/批量/站内信） | P1 | 管理端-用户 | 未开始 |
| F06 | 订单管理增强（手动标记支付/改单） | P1 | 管理端-订单 | 未开始 |
| F07 | 优惠券批量生成 | P1 | 管理端-优惠券 | 未开始 |
| F08 | 审计日志查询界面 | P1 | 管理端-系统 | 未开始 |
| F09 | 节点批量操作/复制/排序 | P1 | 管理端-节点 | 未开始 |
| F10 | 订阅模板管理 | P1 | 订阅 | 未开始 |
| F11 | 邮件模板管理 | P2 | 通知 | 未开始 |
| F12 | Telegram 机器人集成 | P2 | 通知/账户 | 未开始 |
| F13 | 快速登录（邮箱一键登录） | P2 | 账户 | 未开始 |
| F14 | 会话管理界面（用户端） | P2 | 账户 | 未开始 |
| F15 | 公告/知识库排序与分类管理 | P2 | 内容 | 未开始 |
| F16 | 流量重置管理 | P2 | 管理端-流量 | 未开始 |
| F17 | 节点机器管理 | P2 | 运维 | 未开始 |
| F18 | 节点路由 | P2 | 节点 | 未开始 |
| F19 | 主题系统 | P2 | 前端 | 未开始 |
| F20 | 在线更新 | P2 | 部署 | 未开始 |
| F21 | 插件系统 | P2 | 架构 | 未开始 |
| F22 | 安全部署项（safe_mode/路径定制） | P2 | 部署 | 未开始 |

> 状态列随实现推进更新；完成项在标题加 ✅。

## 4. 需求明细

### F01 礼品卡/兑换码系统（P0）

**现状**：YLink 无任何卡密能力。Xboard 具备完整 GiftCard 模块（模板 CRUD、批量生成、CSV 导出、启停、用量统计；用户端 check/redeem/history/detail/types）。

**目标行为**：
1. 管理端：礼品卡模板 CRUD（面额、周期、使用限制）；批量生成卡密（数量、前缀规则）；导出 CSV；启用/停用；用量与兑换统计（张数、金额、剩余）。
2. 用户端：输入卡密兑换（按面额充值余额，或直接开通套餐）；兑换历史；卡密类型查询。
3. 面向场景：代理/分销渠道发卡、线下售卖、活动赠送。

**涉及模块**：`server`（新表 `gift_card_templates`/`gift_cards`/`gift_card_usages`，服务、handler、路由）、`src/views`（管理端 + 用户端页面）、`mock`、`docs/api`。

**验收要点**：
- 批量生成幂等（重复提交不产生重复卡密）；卡密一次性兑换（行锁防并发双花）；兑换后余额变动入流水；停用卡密不可兑换。
- 管理端 CSV 导出字段与 `docs/api` 契约一致。

---

### F02 佣金提现（工单提现）（P0）

**现状**：YLink 佣金只能划转为余额（`POST /invite/transfer`），无法提现。

**目标行为**：
1. 系统设置：提现开关（可关闭）、提现方式白名单（如 USDT-TRC20/支付宝/银行卡，可配置）、最低提现额、每次/每日限额。
2. 用户端：工单新增「佣金提现」类型（或独立提现入口）；提交时填提现方式+收款账号；校验佣金余额 ≥ 最低提现额；系统自动生成提现工单并**冻结/扣减**对应佣金。
3. 管理端：工单列表区分提现类型，可标记「已打款」完成提现；拒绝则佣金退回。

**涉及模块**：`server`（ticket 类型扩展、佣金冻结字段、设置项）、工单前端、管理端工单页、`docs/api`。

**验收要点**：
- 拒绝/关闭提现工单必须自动退回冻结佣金（状态机与退款一致，防资损）。
- 重复提交相同提现被防重/限额拦截。

---

### F03 多支付渠道接入（P0）

**现状**：仅易支付驱动（`epay_alipay`/`epay_wxpay`）+ 余额支付。Xboard 有 AlipayF2f、Btcpay、Coinbase、CoinPayments、Epay、Mgate、Stripe。

**目标行为**：
1. 支付抽象层增加驱动注册表扩展能力（已具备 `pkg/payment.Driver` 接口，新增驱动即可）。
2. 优先接入：支付宝当面付（AlipayF2f，国内主要市场）、USDT 类（Coinbase/CoinPayments/Btcpay 三选一，海外/匿名场景）。
3. 管理端支付配置页：动态启停渠道、配置密钥、排序、手续费展示；用户端订单页展示可用支付方式（`/config` 已下发支付方式，扩展即可）。
4. `POST /payment/notify/{method}` 保持统一验签入口，各驱动自实现验签与查单。

**涉及模块**：`server/internal/pkg/payment`、settings、`docs/api`、前端订单/管理端设置页。

**验收要点**：
- 每种新渠道走通「下单 → 拉起 → 回调验签 → 开通续期」全链路；回调金额比对单位约定（分）不破。
- 未配置密钥的渠道自动隐藏，不影响其余渠道。

---

### F04 管理端统计报表增强（P1）

**现状**：仅有 `GET /admin/stat/overview` 单一概览。Xboard 有订单统计、节点排行（昨日/上次）、流量排行、用户留存、综合报表（getStats 等 8 组接口）。

**目标行为**（按落地成本递增）：
1. 订单统计：按日/周/月的订单数、营收（区分支付渠道）、退款额趋势图。
2. 节点排行：昨日流量 TopN、在线/离线状态。
3. 用户统计：注册趋势、套餐分布、活跃/沉睡用户。
4. 流量排行：用户流量消耗 TopN、节点流量分布。

**涉及模块**：`server`（统计聚合 SQL + 新端点）、前端新增「报表」管理页面、`docs/api`。

**验收要点**：聚合口径与 `traffic_logs`/`orders` 数据一致；时间范围参数化（默认近 30 天），避免全表扫描（走索引/物化统计）。

---

### F05 用户管理增强（P1）

**现状**：管理端用户仅有列表/编辑/调余额/封禁。

**目标行为**：
1. CSV 导出（按筛选结果，字段：邮箱/余额/佣金/套餐/到期/流量/注册时间/邀请人）。
2. 批量操作：批量封禁/解封、批量调整余额（含负值保护）、批量发送通知。
3. 站内信/邮件：管理端向指定用户发邮件（复用 SMTP，记录发送日志）。
4. 重置用户订阅密钥（Xboard resetSecret 等价，YLink 已有 `POST /servers/:id/node-key/reset` 的节点侧，补用户侧）。

**涉及模块**：`server` admin 端点扩展、`src/views/admin/AdminUsersView.vue`、`docs/api`。

**验收要点**：导出走流式/分页防内存峰值；批量操作有结果汇总（成功数/失败原因）。

---

### F06 订单管理增强（P1）

**现状**：管理端订单仅关闭/退款。

**目标行为**：
1. 手动标记已支付（`paid`）：用于线下/人工收款后补录，执行与回调同一条开通状态机（含佣金、优惠券记账），防资损校验（金额一致、未超时关单可标记）。
2. 改单（`update`）：备注、支付方式修正等受控字段；金额修改需二次确认并留审计。

**涉及模块**：`server` order admin 端点、`AdminOrdersView.vue`、`docs/api`。

**验收要点**：手动标记支付必须复用 MarkPaid 幂等/事务逻辑，不能另写一套导致竞态；操作写 audit_logs。

---

### F07 优惠券批量生成（P1）

**现状**：`POST /admin/coupons` 逐个创建。

**目标行为**：管理端按「数量 + 金额/折扣 + 有效期 + 限制」批量生成；支持导出卡密表（区分正式优惠券与卡密，卡密归 F01）；批量幂等。

**涉及模块**：`server`（`GET/POST /admin/coupons/generate`）、`AdminCouponsView.vue`。

**验收要点**：一次生成上限（如 1000）；生成记录可追溯；与下单占用逻辑（原子占用）兼容。

---

### F08 审计日志查询界面（P1）

**现状**：`audit_logs` 表已写入（封禁/调余额/关单/退款/代理审批/流量导入/node-key 重置等），但管理端无可查界面，等同「只写不读」。

**目标行为**：管理端新增「审计日志」页：按操作人/动作/目标/时间范围筛选、分页、详情（含请求 IP 与变更明细）；仅有管理员可访问。

**涉及模块**：`server`（`GET /admin/audit-logs` 只读端点）、`AdminAuditLogsView.vue`、`docs/api`。

**验收要点**：数据源直接用现有 `audit_logs` 表，无需新增写入逻辑。

---

### F09 节点批量操作/复制/排序（P1）

**现状**：节点仅有单条 CRUD + 分组 + node-key 重置，配置存 JSON。

**目标行为**：
1. 批量：删除、启停、重置流量、更新公共字段（协议/速度限制等）。
2. 复制节点（快速开通同规格新节点）。
3. 节点排序（前端展示顺序，Xboard sort 等价）。

**涉及模块**：`server`（批量端点）、`AdminNodesView.vue`、`docs/api`。

**验收要点**：批量操作带事务与结果汇总；复制节点生成新 node-key 与订阅凭证，不共享。

---

### F10 订阅模板管理（P1）

**现状**：订阅内容由 `pkg/subscribe` 代码内固定生成（Clash YAML / sing-box JSON / v2ray base64）。

**目标行为**：管理端可维护订阅模板（按客户端类型），支持变量占位（节点列表、用户信息头、提示节点）；未配置模板时回退内置生成器，保证不破坏现有订阅兼容性。

**涉及模块**：`server`（模板表 + 渲染）、`AdminSettingsView.vue` 或新页面、`docs/api`。

**验收要点**：模板语法错误不导致订阅 500，回退默认模板并告警。

---

### F11 邮件模板管理（P2）

**现状**：验证码/到期提醒/流量提醒邮件文案硬编码。

**目标行为**：管理端邮件模板列表/编辑/恢复默认/测试发送（复用 Xboard MailTemplate 模式）；占位符（`{site_name}`/`{username}`/`{code}` 等）文档化。

**涉及模块**：`server`（`mail_templates` 表 + 渲染）、`AdminSettingsView.vue`、`docs/api`。

**验收要点**：测试发送走真实 SMTP；模板缺失自动回退内置文案。

---

### F12 Telegram 机器人集成（P2）

**现状**：用户表仅存 `telegram_id`，无机器人交互。

**目标行为**（最小闭环）：
1. 管理端配置 bot token + webhook（`setTelegramWebhook`）。
2. 用户绑定/解绑（发送 `/bind <验证码>` 给 bot，或用户端填入 chat id）。
3. 通知推送：到期/流量提醒邮件的同时推 Telegram（复用现有 remind 定时任务的分发层）。

**涉及模块**：`server`（telegram 客户端 + webhook 端点 + 绑定流程）、`ProfileView.vue`、设置页、`docs/api`。

**验收要点**：webhook 签名校验；解绑后立即停止推送；推送失败不影响主流程（降级记日志）。

---

### F13 快速登录（邮箱一键登录）（P2）

**现状**：注册/登录/找回均需密码或验证码重置，无「免密登录」。

**目标行为**：`POST /auth/mail-login`：用户提交邮箱 → 发送一次性登录链接（含短期 token，单次有效）→ 点击即登录并签发 JWT；与快速登录 URL 生成（客服代登）共用 token 机制，仅限管理员生成。

**涉及模块**：`server` auth 扩展、`LoginView.vue`、`docs/api`。

**验收要点**：token 单次使用、短时效（如 10min）；被代登用户会话版本号 bump，防 token 泄露后持续有效。

---

### F14 会话管理界面（用户端）（P2）

**现状**：后端已有 refresh 旋转 + SV 实时失效，但用户不可见、不可控。

**目标行为**：用户端「安全」页展示活跃会话（设备/时间/IP），支持踢下线指定会话；后端在 Redis 白名单维度支持按会话 jti 删除。

**涉及模块**：`server`（`GET/DELETE /user/sessions`）、`ProfileView.vue` 或新页面、`docs/api`。

**验收要点**：踢下线后该会话 access 立即失效（SV/白名单联动），其余会话不受影响。

---

### F15 公告/知识库排序与分类管理（P2）

**现状**：公告/知识库无排序字段；知识库无分类管理（现按 `language` 聚合）。

**目标行为**：内容模型增加 `sort` 排序字段与拖拽排序接口；知识库分类 CRUD（Xboard getCategory/sort 等价）；管理端排序交互。

**涉及模块**：`server`（sort/category 字段 + 迁移）、`AdminNoticesView.vue`/`AdminKnowledgesView.vue`、`docs/api`。

**验收要点**：排序即时生效并作用于用户端展示顺序；迁移对存量数据默认值兜底。

---

### F16 流量重置管理（P2）

**现状**：仅 `POST /admin/traffic/import` 手动导入流量；Xboard 有重置记录/统计/按用户重置/历史。

**目标行为**：管理端按用户/批量重置流量（清零 u/d 或重新给量），保留重置记录与统计（新表 `traffic_reset_logs`），可查单用户历史。

**涉及模块**：`server` + `AdminTrafficImportView.vue` 扩展（或并入用户管理）、`docs/api`。

**验收要点**：重置操作与节点上报差分幂等兼容（清快照防重复计费）。

---

### F17 节点机器管理（P2）

**现状**：`server/docs/node-agent-guide.md` 描述了对接方式，实操靠手工；Xboard Machine 有安装命令生成/负载历史。

**目标行为**：管理端「机器」页：登记机器、生成一键安装命令（含 node-key）、查看机器节点挂载与负载历史（网络/CPU/内存，由现有 node report 扩展上报）。

**涉及模块**：`server`（machine/report 扩展）、新管理页面、`docs/api`。

**验收要点**：安装命令幂等可重跑；历史负载数据有保留周期与聚合降采样。

---

### F18 节点路由（P2）

**现状**：无路由概念（Xboard `v2_server_route`：按用户/套餐/地区路由到不同节点集群）。

**目标行为**：评估是否需要（当前按套餐 `group_ids` 分组已覆盖主要场景）；如立项：路由规则 CRUD + 下发优先级 + 测试页面。

**涉及模块**：`server`、节点管理。
**默认决策**：本期**不立项**，保留为远期（见 §6 待决策），避免引入未验证的复杂度。

---

### F19 主题系统（P2）

**现状**：单一 SPA 固定 UI。

**目标行为**：评估换肤成本。可行最小方案：管理端可配「品牌配置」（主色/logo/背景/站点名，部分 Xboard theme_config 能力），而非完整多主题上传切换；完整主题包机制需前端架构重构（组件风格抽离），成本高。

**涉及模块**：前端 UnoCSS 设计令牌（`docs/frontend/design-system.md` 已有基础）、设置页。
**默认决策**：先做「品牌配置」子集，完整主题系统**缓排**（见 §6）。

---

### F20 在线更新（P2）

**现状**：无升级通道。

**目标行为**：管理端检查版本 + 展示升级说明；执行升级需人工确认并备份（后端决策：本项目后端不走 CI，升级 = 拉取镜像/代码 + 迁移脚本；可先做「版本检查 + 变更日志」展示，不做半自动执行）。

**涉及模块**：`server`（version 端点）、新管理页面。
**默认决策**：先做版本检查与变更日志展示，自动执行**不立项**（风险高、与部署流程耦合深）。

---

### F21 插件系统（P2）

**现状**：无插件架构，支付驱动为注册表模式（已有扩展点）。

**目标行为**：完整插件机制（安装/卸载/启停/配置/上传）是架构级改造，收益主要在第三方支付与渠道定制。当前支付 Driver 注册表 + 配置化已覆盖主要变体。

**默认决策**：**不立项**，保留注册表 + 配置化路线；仅在出现真实第三方扩展需求时重估。

---

### F22 安全部署项（P2）

**现状**：后台路径固定 `/admin`，无域名白名单模式。

**目标行为**（低成本高收益）：
1. 后台路径可配置（`secure_path`，启动注入，避免写库后需重启的路径问题）。
2. `safe_mode`：`app_url` 域名白名单校验（非白名单域名拒绝访问，尤其用户端绑定域名防仿冒），Xboard 首页已实现同逻辑。
3. 订阅路径可定制（`subscribe_path`，默认 `s`）。

**涉及模块**：`server` config/settings + `router`、部署文档。

**验收要点**：路径/域名配置变更文档化重启步骤；订阅端点与回调端点在路径变更后仍正确。

---

## 5. 总体验收标准

1. 每个需求按「验收要点」逐项通过；后端 `go test ./... -count=1` 全绿，前端 `pnpm lint/typecheck/test` 通过。
2. 新端点/字段同步进 `docs/api/README.md` 契约与 `docs/backend/data-model.md`；新表走 golang-migrate SQL（含 down）。
3. 涉及资金操作（F01 兑换、F02 提现、F06 手动支付）必须有幂等、行锁与 audit 审计。
4. 用户端/管理端页面按 `docs/frontend/design-system.md` 规范实现；E2E 关键流程补充 Playwright 用例。
5. 完成项在本文档标题/条目标注 ✅，进度同步到 `docs/backend/progress.md` 与 `docs/frontend/progress.md`。

## 6. 待决策问题

| # | 问题 | 选项 | 建议 |
|---|---|---|---|
| D1 | P0 三项（F01/F02/F03）的推进顺序与排期 | 先卡密 / 先提现 / 先支付渠道 | 建议 F03（支付渠道）与 F01（礼品卡）并行，F02 紧随 |
| D2 | F17/F18 节点机器管理与路由是否本期立项 | 立项 / 缓排 | 缓排（当前分组已覆盖主要场景） |
| D3 | F19/F20/F21（主题/更新/插件）是否彻底不做 | 品牌配置子集 / 完整做 / 不做 | 品牌配置子集，其余不做 |
| D4 | 多支付渠道优先接哪家 | 支付宝当面付 / USDT（Coinbase 等） | 视用户市场：国内场景支付宝当面付优先 |

## 7. 参考

- Xboard 功能盘点结论：本对话前一轮对比分析（控制器方法/db 表/插件目录三来源）
- YLink 现状：`server/internal/router/router.go`、`docs/backend/progress.md`、`docs/api/README.md`
- Xboard 源码：`Z:\web_workplace\Xboard-Code`（app/Http/Controllers、database/migrations、plugins-core、theme）