# YLink 环境变量模板(无敏感信息,可入库)
# 使用方式:
#   本地开发: cp .env.example .env.dev   (scripts/dev.sh 读取;DSN 会被脚本以宿主机 127.0.0.1:5433 覆盖,无需手动改)
#   生产发布: cp .env.example .env.release(ENV_FILE=.env.release docker compose up -d;DSN 用容器服务名 postgres)
# 注意:.env.dev / .env.release 均已被 .gitignore 忽略,不会入库。
# APP_ENV:生产发布保持 production(关 Swagger/debug);本地开发经 scripts/dev.sh 启动时会被强制覆盖为 development(开 Swagger/debug)。
APP_ENV=production
APP_BASE_URL=http://localhost:8081
APP_DATABASE_DSN=host=postgres port=5433 user=ylink password=ylink_root dbname=ylink-backend sslmode=disable TimeZone=Asia/Shanghai
APP_REDIS_ADDR=redis:6379
APP_REDIS_PASSWORD=ylink_redis
APP_JWT_SECRET=please-change-me-32bytes-secret-key!!
APP_SMTP_HOST=smtp.qq.com
APP_SMTP_PORT=465
APP_SMTP_USERNAME=
APP_SMTP_PASSWORD=
APP_PAYMENT_EPAY_GATEWAY=https://pay.example.com
APP_PAYMENT_EPAY_PID=
APP_PAYMENT_EPAY_KEY=

POSTGRES_USER=ylink
POSTGRES_PASSWORD=ylink_root
POSTGRES_DB=ylink-backend
REDIS_PASSWORD=ylink_redis

# 首个管理员（服务启动时幂等创建）
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=Admin@123456

# 演示账号（服务启动时幂等创建;dev-docker.sh 亦从 .env.dev 读取）
DEMO_EMAIL=demo@test.com
DEMO_PASSWORD=Passw0rd

# 可观测性告警收件邮箱(可选;启用 `--profile obs` 的 alertmanager 时生效,
# SMTP 复用上方 APP_SMTP_*;未配置时默认发到 admin@example.com,生产必须显式配置)
ALERT_EMAIL_TO=
