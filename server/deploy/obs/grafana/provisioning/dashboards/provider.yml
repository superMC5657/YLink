# Grafana 看板自动加载(provisioning,启动即挂载 dashboards/ 下的 JSON)
apiVersion: 1

providers:
  - name: ylink
    orgId: 1
    folder: YLink
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
